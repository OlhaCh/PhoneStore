<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0.3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ListView Grid.Row="1">
            <StackPanel>
                <TextBlock Name="ModelTxtBlock" HorizontalAlignment="Left" Text="{Binding Path=PhoneView.Model}"/>
                <TextBlock Name="CostTxtBlock" HorizontalAlignment="Center" Text="{Binding Path = PhoneView.Cost}"/>
                <Button Name="OrderButton" HorizontalAlignment="Right" Content="Buy" FontWeight="Bold" FontSize="16" />
            </StackPanel>
        </ListView>
        <Button Grid.Column="1" Name="LoginButton" Content="Log In" FontSize="18" FontWeight="Bold" Click="LoginButton_Click"/>
    </Grid>
</Window>
